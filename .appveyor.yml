version: 1.0.{build}
image: Ubuntu2004
environment:
  matrix:
    - nodejs_version: "18"
install:
  - ps: Install-Product node $env:nodejs_version
  - cd 1.health-indicator
  - npm install
build: off
test_script:
  - cd 1.health-indicator
  - npm run lint
  - npm test